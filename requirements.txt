# Core UI framework
streamlit>=1.38.0

# PDF parsing for page counts / later RAG ingestion
pypdf>=4.0.0

# RAG stack (ahead of Step 2)
langchain>=0.3.0
langchain-community>=0.3.0
chromadb>=0.5.5
openai>=1.40.0

# Env management (local dev only)
python-dotenv>=1.0.1

# NOTE:
# - sqlite3 ships with Python, so no extra package needed for local DB
# - We'll add SQLModel / SQLAlchemy later when wiring persistence


pypdf==5.0.1
pdfplumber==0.11.4
sqlalchemy==2.0.34
tiktoken==0.7.0
